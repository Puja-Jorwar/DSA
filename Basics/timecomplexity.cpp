#include <bits/stdc++.h>
using namespace std;

// time taken by code to run is not time complexity.
// the rate at which the time taken increses with respect to input size.
// Rules
//->TC,worst case
//->avoid constant
//->avoid lower values
// n,nsqu,summetion formula

int main()
{
    return 0;
}

//Big-oh(worst), theta(ave),omega(best case)