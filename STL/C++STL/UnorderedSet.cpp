#include <bits/stdc++.h>
using namespace std;

// Unordered set -> stores uniwue but not sorted
//O(1)
//in very worst case (rare) it will be O(n) very rare
void explainUSet()
{
    unordered_set<int> st;
    // lower_bound and upper_bound function does not wors, rest all functions are same as set,it does not stored in any particular order it has better complexit than set in most cases, except some when collision occurs
}

int main()
{
    return 0;
}